{
  "contract_version": "1.1",
  "frozen_on": "2026-09-21",
  "purpose": "Prevent partial Ask the Model release/version updates by requiring the public version and release date to move atomically across all version-bearing public metadata before Flatpak build or GitHub Release publication.",
  "versioning_basis": {
    "scheme": "Semantic Versioning",
    "source": "https://semver.org/",
    "current_series": "0.x initial development",
    "policy": "Release version changes are deliberate. A minor release is appropriate when substantial backward-compatible user-facing functionality is added; patch releases are reserved for compatible corrections."
  },
  "current_public_release": {
    "version": "0.3.0",
    "date": "2026-09-21",
    "version_bump_required_now": false,
    "reason": "The repository-aware GTK path is now user-facing and locally smoke-tested: fixed EWD/CBD/RMD lifecycle, deterministic retrieval, per-chat pinning, grounded citations and exact-SHA provenance materially extend the v0.2 local-chat baseline."
  },
  "canonical_sources": {
    "version": "meson.build project() version",
    "release_date": "CITATION.cff top-level date-released"
  },
  "synchronized_surfaces": [
    {
      "path": "meson.build",
      "rule": "project version is the canonical application version"
    },
    {
      "path": "CITATION.cff",
      "rule": "top-level version and date-released match the canonical release"
    },
    {
      "path": "README.md",
      "rule": "primary release badge alt text exposes the exact current version"
    },
    {
      "path": "STATUS.md",
      "rule": "Current release line contains the exact version and release date"
    },
    {
      "path": "CHANGELOG.md",
      "rule": "released section heading is '## VERSION - YYYY-MM-DD'"
    },
    {
      "path": "releases/vVERSION.md",
      "rule": "file exists; title and release subtitle/date match the exact release"
    },
    {
      "path": "data/io.github.laurentiustaicu.ask_the_model.metainfo.xml",
      "rule": "the first AppStream <release> entry matches the exact version and release date"
    }
  ],
  "deliberately_not_application_version": {
    "path": "data/io.github.laurentiustaicu.ask_the_model.desktop",
    "field": "Version=1.0",
    "reason": "This field declares the Desktop Entry file-format/specification version and must not be rewritten to the AtM application version."
  },
  "workflow_gate": {
    "script": "scripts/validate_release_metadata.sh",
    "workflow": ".github/workflows/flatpak.yml",
    "placement": "before README validation and Flatpak build",
    "release_publisher": "The existing workflow derives the tag from meson.build and requires releases/vVERSION.md before publishing.",
    "failure_policy": "Any version/date mismatch fails before the expensive Flatpak build and before release publication."
  },
  "future_release_rule": {
    "repository_grounded_chat": "Repository selection, exact-SHA snapshots, grounded context and inspectable citations are public v0.3.0 functionality and must remain covered by release documentation and regression gates.",
    "patch_documentation": "Documentation-only or compatible bug corrections may use a patch release when deliberately published as a new immutable release.",
    "new_capability": "Substantial new user-facing capability should normally prepare a new 0.x minor release while the public API remains pre-1.0.",
    "released_versions_are_immutable": true
  }
}
